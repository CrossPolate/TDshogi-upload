; 项目级 npm 配置
;
; ignore-scripts=true —— 关键修复：
; better-sqlite3 v13 tarball 已自带全平台预编译二进制（prebuilds/*.node），
; 运行时通过 node-gyp-build 加载，无需本地编译；
; 但它带有 binding.gyp 且未声明 install 脚本，npm 会自动执行 node-gyp rebuild，
; 在缺少 Visual Studio C++ 工具链/Windows SDK（或 Linux 无 build-essential）的
; 部署机上整包安装失败 —— 这是「服务器无法部署」的直接原因。
; 本项目全部依赖（express/ws/shogi.js 纯 JS）均不需要生命周期脚本，
; 因此在此全局关闭，保证任意机器 npm install/ci 都能成功。
; 注意：若未来引入需要编译或 postinstall 的依赖，需重新评估此选项。
ignore-scripts=true
